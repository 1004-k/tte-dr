# Source-code layout note for SoftwareX

TTE-DR is an R project. Following R package convention, source functions are stored in `R/` rather than `src/`. The `scripts/` directory contains executable entry points for examples, quick checks, and full reproducibility runs. This file is included so that reviewers looking for a SoftwareX-style `src/` directory can locate the source-code layout quickly.
